
## Statistical module

The **StatisticalModule** module handles requests to fetch data and run data analysis.
Subsequently, it returns the final statistical reports back to the UI. 
This module interacts with the database storing current active information about enrollemnts, the archive containing historical information about enrollment changes, as well as the rest of the SIS database.

![](embed:StatisticalModule)