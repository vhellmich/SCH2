## Users DB

**Purpose**  
Acts as the source of truth for user accounts, credentials, and session metadata.

**Collaborations**
- Accessed and maintained by `Authentication Business`.
- Queried by `Authorizer` when evaluating permissions.

